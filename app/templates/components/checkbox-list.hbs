{{#each items as |item|}}
	{{yield (hash
  	item=item
    checkbox=(component "checkbox-list-checkbox"
    	item=item
      checkedItemsSet=checkedItemsSet
      click=(action "onCheck" item)
    )
  )}}
{{/each}}