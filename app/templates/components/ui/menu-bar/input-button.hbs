<i class="{{icon}} icon"></i>
<input class="file-upload menu menu-button primary button"
       accept={{accept}}
       type="file"
       value={{label}}
       on="change"
       onchange={{action compAction}}>