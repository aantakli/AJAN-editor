{{#ui/tool-tip tooltip=tooltip}}
  <div class="ui {{if icon "icon"}} input full-width">
    {{#if icon}}
      <i class="{{icon}} icon"></i>
    {{/if}}

    {{input
      id=inputID
      placeholder=placeholder
      readonly=readonly
      value=value
      onChange=(mut value)
      type=type
    }}
  </div>
{{/ui/tool-tip}}
