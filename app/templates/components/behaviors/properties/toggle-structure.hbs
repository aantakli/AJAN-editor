{{toggleValue}}
{{#if toggleValue}}
  {{component 'behaviors/properties/toggle-item'
    node=node
    item=toggle.first
    class="flexed-input"
  }}
{{else}}
  {{component 'behaviors/properties/toggle-item'
    node=node
    item=toggle.last
    class="flex-fill-item"
  }}
{{/if}}
